{
  "name": "rate-ninja",
  "version": "1.0.0",
  "private": true,
  "description": "Ocean freight rate lookup application",
  "scripts": {
    "start": "node server.js",
    "migrate": "node scripts/migrate-from-airtable.js",
    "check": "node scripts/check-syntax.js",
    "set-password": "node scripts/set-password.js",
    "test": "node --experimental-test-isolation=process --test test/**/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2"
  }
}
