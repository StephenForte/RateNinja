{
  "name": "rate-ninja",
  "version": "1.0.0",
  "private": true,
  "description": "Ocean freight rate lookup application",
  "scripts": {
    "start": "node server.js",
    "migrate": "node scripts/migrate-from-airtable.js",
    "check": "node --check server.js && node --check script.js && node --check lib/config.js && node --check lib/airtable.js && node --check lib/domain.js && node --check lib/session.js && node --check lib/db.js && node --check scripts/migrate-from-airtable.js"
  },
  "engines": {
    "node": ">=22"
  }
}
